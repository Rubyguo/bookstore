<p id="notice"><%= notice %></p>

<h1>Listing Books</h1>

<table>
  <thead>
    <tr>
      <th>Book name</th>
      <th>Book type</th>
      <th>Author</th>
      <th>Words num</th>
      <th>Source</th>
      <th>Bookintro</th>
      <th>Cover path</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.book_name %></td>
        <td><%= book.book_type %></td>
        <td><%= book.author %></td>
        <td><%= book.words_num %></td>
        <td><%= book.source %></td>
        <td><%= book.bookintro %></td>
        <td><%= book.cover_path %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
